---
version: "1.0.0"
last_updated: "2026-05-10"
updated_by: ""
---

# システム構成図

> **参照元（業務要件定義資料）:**
> - 業務一覧.md（システム化対象業務の特定）
> - 業務プロセス定義.md（システム構成要素の役割・責務）
> - ユースケース定義.md（システム利用者・利用シーン）
> - 役割分担定義.md（システムと人の分担）

## システム全体構成図

```mermaid
flowchart TD
    User["👤 一般社員\n（CLIユーザー）"]

    subgraph App["申請書自動作成システム（Python / AWS Strands SDK）"]
        Main["main.py\nエントリーポイント"]

        subgraph Agents["エージェント層"]
            AG001["AG-001\n申請受付窓口エージェント\n（オーケストレーター）"]
            AG002["AG-002\n交通費精算申請エージェント\n（専門エージェント）"]
            AG003["AG-003\n経費精算申請エージェント\n（専門エージェント）"]
        end

        subgraph Tools["ツール層"]
            TOOL001["TOOL-001\n交通費計算ツール"]
            TOOL002["TOOL-002\n申請書生成ツール"]
        end

        subgraph Handlers["ハンドラー層"]
            ErrHandler["ErrorHandler\nエラーハンドリング"]
            LoopHook["LoopControlHook\nループ制御"]
            ApprovalHook["HumanApprovalHook\n承認ゲート"]
        end

        subgraph Data["データ層"]
            TrainFares["data/train_fares.json\n経路テーブル"]
            FixedFares["data/fixed_fares.json\n固定運賃データ"]
            Templates["template/\n申請書テンプレート"]
            Sessions["storage/sessions/\nセッションデータ"]
            Output["output/\n生成済み申請書"]
            Logs["logs/\nアプリログ"]
        end
    end

    subgraph AWS["Amazon Web Services"]
        Bedrock["Amazon Bedrock\nClaude Sonnet\n（LLM推論基盤）"]
    end

    User -->|"申請内容入力"| Main
    Main -->|"会話ループ"| AG001
    AG001 -->|"委譲（Agent as Tools）"| AG002
    AG001 -->|"委譲（Agent as Tools）"| AG003
    AG002 -->|"ツール呼び出し"| TOOL001
    AG002 -->|"ツール呼び出し"| TOOL002
    AG003 -->|"ツール呼び出し"| TOOL002

    TOOL001 -.->|"参照"| TrainFares
    TOOL001 -.->|"参照"| FixedFares
    TOOL002 -.->|"参照"| Templates
    TOOL002 -.->|"書き込み"| Output

    AG001 -.->|"セッション管理"| Sessions
    AG002 -.->|"セッション管理"| Sessions
    AG003 -.->|"セッション管理"| Sessions

    App -.->|"ログ出力"| Logs

    AG001 -->|"LLM推論（SDK経由）"| Bedrock
    AG002 -->|"LLM推論（SDK経由）"| Bedrock
    AG003 -->|"LLM推論（SDK経由）"| Bedrock

    LoopHook -.->|"フック"| AG001
    LoopHook -.->|"フック"| AG002
    LoopHook -.->|"フック"| AG003
    ApprovalHook -.->|"承認ゲート"| AG002
    ApprovalHook -.->|"承認ゲート"| AG003
    ErrHandler -.->|"エラー処理"| App
```

## 凡例
- 実線矢印（→）：処理の委譲・呼び出し
- 破線矢印（-.->）：データの参照・取得・フック
